# private correspondence — never commit
*.pdf
# generated demo script is ours and safe to publish
!DEMO.pdf

# secrets
pipeline/.env
.env

# bulky per-call API caches (raw jsonl is the canonical artifact)
data/raw/cache/

# local tooling state
.gstack/
.claude/
.DS_Store
nohup.out
__pycache__/
*.pyc
